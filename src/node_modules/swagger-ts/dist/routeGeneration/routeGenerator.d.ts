import { Metadata } from '../metadataGeneration/metadataGenerator';
export declare class RouteGenerator {
    private metadata;
    private routeDir;
    constructor(metadata: Metadata, routeDir: string);
    GenerateRoutes(middlewareTemplate: string): Promise<void>;
    GenerateExpressRoutes(): Promise<void>;
    private buildContent(middlewareTemplate);
    private getModels();
    private getStringRepresentationOfType(type);
    private getRelativeImportPath(controllerLocation);
    private getTemplateProperty(source);
    private getExpressPath(path);
}
